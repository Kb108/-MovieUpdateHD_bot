services:
  - type: web
    name: movieupdatehd-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn indexer:app  # অথবা python indexer.py (যদি Flask না হয়)
    plan: free
